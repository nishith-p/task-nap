import { Layout } from "../components/Layout";
import { ProjectList } from "../components/ProjectList";

export const Home = () => {
  return (
    <Layout>
      <ProjectList />
    </Layout>
  );
};
